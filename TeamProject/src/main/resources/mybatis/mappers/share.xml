<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ilinbun.mulcam.dao.ShareDAO">	
	<select id="selectShareboard" resultType="Shareboard">
		<![CDATA[
			SELECT * FROM shareboard ORDER BY articleno DESC LIMIT 1 
		]]>
	</select>
	<insert id="insertShareBoard" parameterType="Shareboard">
 		<![CDATA[
 			insert into team2.shareBOARD (idx, subway, title, content)
 			
 			values (#{idx}, #{subway}, #{title}, #{content})
 		]]>
 		
 	</insert>
	<update id="updateShareBoard" parameterType="Shareboard">
 		<![CDATA[
 			update team2.shareBOARD set title=#{board_subject}, content=#{content}
 			where articleNo=#{articleNo}
 		]]>
 	</update>
 	<update id="updateReadCount" parameterType="int">
 		<![CDATA[
 			update team2.shareBOARD set readCount=readCount+1 where articleNo=#{articleNo} 
 		]]>
 	</update>

</mapper>